// Tabs tsconfig
{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    // Tabs should never need to emit any kind of files
    "declaration": false,
    "noEmit": true,

    "jsx": "react-jsx"
  }
}

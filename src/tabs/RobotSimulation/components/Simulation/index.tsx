import { type CSSProperties } from 'react';

const CanvasWrapperStyle: CSSProperties = {
  width: 800,
  height: 600,
  backgroundColor: 'white',
};

export default function SimulationCanvas() {
  return <div style={CanvasWrapperStyle}>hi</div>;
}

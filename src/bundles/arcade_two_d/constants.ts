// The default values. These values will likely change.

// Default values of canvas
export const DEFAULT_WIDTH: number = 600;
export const DEFAULT_HEIGHT: number = 600;
export const DEFAULT_SCALE: number = 1;
export const DEFAULT_FPS: number = 30;
export const DEFAULT_VOLUME: number = 0.5;

// Maximum allowed values for canvas
export const MAX_HEIGHT: number = 800;
export const MIN_HEIGHT: number = 400;
export const MAX_WIDTH: number = 800;
export const MIN_WIDTH: number = 400;
export const MAX_SCALE: number = 2;
export const MIN_SCALE: number = 0.5;
export const MAX_FPS: number = 60;
export const MIN_FPS: number = 1;
export const MAX_VOLUME: number = 1;
export const MIN_VOLUME: number = 0;

// A mode where the hitboxes is shown in the canvas for debugging purposes,
// and possibly other debug information
export const DEBUG_MODE: boolean = false;

/* eslint-disable max-classes-per-file */
import {
  scrabble_array,
  scrabble_list,
  char_at,
  array_length,
} from './functions';

/**
 * Scrabble words for Source Academy
 * @author Martin Henz
 */

export default () => ({
  scrabble_array,
  scrabble_list,
  char_at,
  array_length,
});

import{_ as o,c as t,o as a,a2 as r}from"./chunks/framework.BYVJ-AET.js";const p=JSON.parse('{"title":"Working With Git","description":"","frontmatter":{"title":"Working With Git"},"headers":[],"relativePath":"modules/1-getting-started/3-git.md","filePath":"modules/1-getting-started/3-git.md","lastUpdated":1770457619000}'),i={name:"modules/1-getting-started/3-git.md"};function s(n,e,c,d,l,h){return a(),t("div",null,e[0]||(e[0]=[r('<h1 id="git-github-workflows" tabindex="-1">Git/Github Workflows <a class="header-anchor" href="#git-github-workflows" aria-label="Permalink to &quot;Git/Github Workflows&quot;">​</a></h1><p>When making changes to the code in this repository, you should abide by some good Git conventions:</p><ul><li>Commit often with informative commit messages</li><li>Run the <code>pre-push</code> Git Hook before pushing so that your code is verified locally</li></ul><div class="tip custom-block github-alert"><p class="custom-block-title">Lockfiles</p><p>Package managers such as <code>yarn</code> and <code>npm</code> use a single file called the lockfile to keep track of the dependencies and their dependencies and so on. Since this repository uses Yarn, the lockfile we use is <code>yarn.lock</code>.</p><p>There is always only one lockfile. Even if you use focused installs or add a dependency to a single package, <code>yarn.lock</code> will be modified.</p><p>When you add a dependency to your bundle/tab, remember to run your installation command to ensure that the lockfile gets updated, and then commit the changes to the lockfile.</p></div><div class="warning custom-block github-alert"><p class="custom-block-title">package-lock.json</p><p>If you accidentally run installation commands with <code>npm</code> instead (i.e <code>npm i</code>), a different type of lockfile will get generated. This file should be deleted if present and should not be added to the repository.</p></div><h2 id="creating-your-own-branch" tabindex="-1">Creating your own branch <a class="header-anchor" href="#creating-your-own-branch" aria-label="Permalink to &quot;Creating your own branch&quot;">​</a></h2><p>The <code>master</code> branch of the repository is protected. This means that you cannot push commits directly to it, nor can pull requests be merged into the branch without first passing code review.</p><p>You shouldn&#39;t be using it as a remote for the development work you do on your own machine. Instead, use <code>git branch</code> (or whatever other mechanism) to create a separate branch off of <code>master</code> for your own use.</p><div class="tip custom-block github-alert"><p class="custom-block-title">Forking</p><p>You can <em>fork</em> the repository, but strictly speaking this is not necessary.</p></div><p>Make sure that the branch is named appropriately. For example, if you were creating a new bundle called <code>matrix</code>, then your branch could be named <code>matrix_bundle</code>.</p><h2 id="procedure-for-pull-requests" tabindex="-1">Procedure for Pull Requests <a class="header-anchor" href="#procedure-for-pull-requests" aria-label="Permalink to &quot;Procedure for Pull Requests&quot;">​</a></h2><p>When you&#39;re ready for your work to be incorporated into the <code>master</code> branch, <a href="https://github.com/source-academy/modules/compare" target="_blank" rel="noreferrer">open</a> a pull request with the modules repository to merge your changes into <code>master</code> branch. Make sure that your branch is up-to-date with the <code>master</code> branch (by performing a <code>git merge</code> from the <code>master</code> branch to your own branch).</p><p>When you open a PR in Github, Github will automatically scaffold the PR for you with some helpful headers and checklists. Do provide an informative description of what your PR intends to achieve as well as mark off the relevant checklist items. You can refer to other PRs (both open as well as ones that have been closed) for further examples of what to write.</p><p>This will automatically trigger the repository&#39;s workflows, which will verify that your changes don&#39;t break any of the existing code. If the workflow fails, check the summary and determine where the errors lie, then fix your code as necessary.</p><p>Once your code has been reviewed, the pull request will be merged.</p><h2 id="opening-issues" tabindex="-1">Opening Issues <a class="header-anchor" href="#opening-issues" aria-label="Permalink to &quot;Opening Issues&quot;">​</a></h2><p>If you have a bug report or feature request, open an issue with the appropriate label(s) <a href="https://github.com/source-academy/modules/issues" target="_blank" rel="noreferrer">here</a></p><h2 id="gitignore-files" tabindex="-1"><code>.gitignore</code> Files <a class="header-anchor" href="#gitignore-files" aria-label="Permalink to &quot;`.gitignore` Files&quot;">​</a></h2><p>You should include files that are produced from compilation into your own <code>.gitignore</code> file and avoid committing them to the repository. By default, the <code>dist</code> folders are ignored for all bundles and tabs, but if you need to add other files you can create a custom <code>.gitignore</code> in your bundle/tab&#39;s directory.</p><div class="tip custom-block github-alert"><p class="custom-block-title">TIP</p><p>If you want to remove a file that been committed in the past you can use <code>git rm</code> or<br><code>git rm --cached</code>.</p></div>',20)]))}const m=o(i,[["render",s]]);export{p as __pageData,m as default};

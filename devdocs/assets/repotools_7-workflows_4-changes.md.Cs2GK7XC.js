import{_ as l,C as r,o as a,c,a2 as i,b as n,w as s,a as o,G as d,a3 as h}from"./chunks/framework.BYVJ-AET.js";const w=JSON.parse('{"title":"Rebuilding Packages","description":"","frontmatter":{"title":"Rebuilding Packages"},"headers":[],"relativePath":"repotools/7-workflows/4-changes.md","filePath":"repotools/7-workflows/4-changes.md","lastUpdated":1776097283000}'),p={name:"repotools/7-workflows/4-changes.md"};function k(g,e,u,E,y,m){const t=r("Mermaid");return a(),c("div",null,[e[3]||(e[3]=i(`<h1 id="determining-when-to-rebuild-a-package" tabindex="-1">Determining When to Rebuild a Package <a class="header-anchor" href="#determining-when-to-rebuild-a-package" aria-label="Permalink to &quot;Determining When to Rebuild a Package&quot;">​</a></h1><p>For this section, it is important to understand the distinction between workspace packages and Yarn packages:</p><table><thead><tr><th></th><th>Workspace</th><th>Yarn</th></tr></thead><tbody><tr><td>Source</td><td>Contained entirely within this repository</td><td>Published to and installed from a remote repository like npm. </td></tr><tr><td>Version range looks like</td><td><code>workspace:^</code></td><td><code>^1.8.1</code></td></tr></tbody></table><p>Specifically, we are concerned with detecting when to rebuild a workspace package.</p><p>There are many workspace packages contained within this repository. It would be entirely unnecessary to rerun building and testing for every single workspace package if changes were only made to one. It thus makes sense to implement some kind of dependency tracking system to rebuild workspace packages only if they were affected by a change.</p><p>Importantly, for the CI/CD pipeline, we are concerned only with changes relative to the <strong>master</strong> branch.</p><p>For each workspace package, there are two main kinds of changes we must detect:</p><ol><li>Non-transitive</li><li>Transitive</li></ol><h2 id="non-transitive-changes" tabindex="-1">Non-Transitive Changes <a class="header-anchor" href="#non-transitive-changes" aria-label="Permalink to &quot;Non-Transitive Changes&quot;">​</a></h2><p>A non-transitive change is the simplest to determine: we can do this by checking if the directory containing the workspace package contains any diffs relative to the master branch. This can be done using the following git command:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --no-pager</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> diff</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --quiet</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  origin/master</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> directory</span></span></code></pre></div><p>This git command exits with a non-zero exit code if it detected changes and zero if it did not. Programatically, this is done by using the <code>@actions/exec</code> library&#39;s <code>getExecOutput</code> function:</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">exitCode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> getExecOutput</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &#39;git&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;--no-pager&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;diff&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;--quiet&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;origin/master&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;--&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, directory],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    failOnStdErr: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ignoreReturnCode: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> exitCode </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><h2 id="transitive-changes" tabindex="-1">Transitive Changes <a class="header-anchor" href="#transitive-changes" aria-label="Permalink to &quot;Transitive Changes&quot;">​</a></h2><p>Transitive changes occur when one of the current workspace package&#39;s dependencies contain changes. For example, the <code>sound</code> bundle relies on the <code>midi</code> bundle, so if the <code>midi</code> bundle has changes (and thus needs to be rebuilt), the <code>sound</code> bundle would also need to be rebuilt, even if the <code>sound</code> bundle did not contain any changes. Since the <code>plotly</code> bundle relies on the <code>sound</code> bundle, it too would also need to be rebuilt.</p>`,15)),(a(),n(h,null,{default:s(()=>[d(t,{id:"mermaid-48",class:"mermaid",graph:"graph%20LR%0A%20%20A%5Bmidi%5D%0A%20%20B%5Bsound%5D%0A%20%20C%5Bplotly%5D%0A%0A%20%20A%20--%20rebuild%20triggers%20--%3E%20B%0A%20%20B%20--%20rebuild%20triggers%20--%3E%20C%0A"})]),fallback:s(()=>e[0]||(e[0]=[o(" Loading... ")])),_:1})),e[4]||(e[4]=i(`<p>However, a workspace package is often dependent on more than just other workspace packages. In fact, most workspace packages also rely on some Yarn packages, which in turn are also dependent on other Yarn packages. Normally, this dependency resolution process is handled automatically by Yarn.</p><p>When a change is made to a workspace package&#39;s dependencies (e.g. upgrading from <code>v1.0</code> to <code>v2.0</code> of <code>gl-matrix</code> to take advantage of new features), that workspace package&#39;s <code>package.json</code> must reflect this change. Then, the <a href="#non-transitive-changes">above</a> method will be able to detect that that workspace package needs to be rebuilt. However, a workspace package&#39;s dependencies can change even if there were no changes to its <code>package.json</code>.</p><p>This can occur because of <a href="https://semver.org" target="_blank" rel="noreferrer">semantic versioning</a>. Often, the version range specified in a <code>package.json</code> doesn&#39;t specify a specific version to be used. For example, the <code>package.json</code> below actually specifies that the <code>curve</code> bundle is compatible with anything that is a minor revision to version 2 of <code>gl-matrix</code>:</p><div class="language-jsonc vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;@sourceacademy/bundle-curve&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;dependencies&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;gl-matrix&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;^2.0.0&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>This is good, because it provides us some flexibility: we&#39;re able to use newer versions of the same package that are mostly compatible but might come with things like bug fixes.</p><p>Now suppose that <code>gl-matrix</code> itself relies on another Yarn package like <code>lodash</code>:</p><div class="language-jsonc vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;gl-matrix&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;dependencies&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;lodash&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;^4.17.0&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Because we can only use 1 version of a package at any time, Yarn has to pick from the list of allowed available versions. In this case the version range allows for <code>4.17.0</code> or <code>4.17.1</code> or several other possible versions of <code>lodash</code>. This process is called &quot;resolution&quot;.</p><p>Once Yarn finishes resolving all of these version ranges, it stores these resolutions in the lockfile. This allows us to use the same resolutions and thus the same versions of packages across multiple machines.</p><p>Suppose that Yarn determines that the version of <code>lodash</code> it wants to use for <code>gl-matrix</code> is <code>4.17.0</code>. This is installed and the lockfile reflects that <code>lodash@^4.17.0</code> got resolved to <code>lodash@4.17.0</code> (note the absence of <code>^</code>).</p>`,10)),(a(),n(h,null,{default:s(()=>[d(t,{id:"mermaid-75",class:"mermaid",graph:"graph%20LR%0A%20%20A%5Bcurve%5D%0A%20%20B%5Bgl-matrix%5D%0A%20%20C%5Blodash%5D%0A%0A%20%20D%5B2.0.0%5D%0A%20%20E%5B4.17.0%5D%0A%0A%20%20A%20--%20requires%20--%3E%20B%0A%20%20B%20--%20resolved%20to%20--%3E%20D%0A%20%20B%20--%20requires%20--%3E%20C%0A%20%20C%20--%20resolved%20to%20--%3E%20E%0A"})]),fallback:s(()=>e[1]||(e[1]=[o(" Loading... ")])),_:1})),e[5]||(e[5]=i("<p>Now, suppose that the publishers of <code>lodash</code> find a minor bug in one of their functions and release a small patch to rectify it. They publish this new version as version <code>4.17.1</code>. Notice that this version is still compatible with the <code>^4.17.0</code> specification.</p><p>When the repository&#39;s automated dependency upgrading bot (Renovate Bot) detects this, it will attempt to cause Yarn to install version <code>4.17.1</code> instead of <code>4.17.0</code> for <code>lodash</code>. It does this by updating the lockfile. But now we have a problem: the <code>curve</code> bundle transitively relies on <code>lodash</code> through <code>gl-matrix</code>.</p>",2)),(a(),n(h,null,{default:s(()=>[d(t,{id:"mermaid-82",class:"mermaid",graph:"graph%20LR%0A%20%20A%5Bcurve%5D%0A%20%20B%5Bgl-matrix%5D%0A%20%20C%5Blodash%5D%0A%0A%20%20D%5B2.0.0%5D%0A%20%20E%5B4.17.0%5D%0A%20%20F%5B4.17.1%5D%0A%0A%20%20A%20--%20requires%20--%3E%20B%0A%20%20B%20--%20resolved%20to%20--%3E%20D%0A%20%20B%20--%20requires%20--%3E%20C%0A%20%20C%20--%20X%20--%3E%20E%0A%20%20C%20--%20resolved%20to%20--%3E%20F%0A"})]),fallback:s(()=>e[2]||(e[2]=[o(" Loading... ")])),_:1})),e[6]||(e[6]=i('<p>The <code>curve</code> bundle&#39;s dependency tree has changed, so we need to rebuild and test it (to make sure that it works with this new version of <code>lodash</code>).</p><p>Yet, because there were no changes made to its <code>package.json</code> or to the code of the <code>curve</code> bundle, the <code>git diff</code> method won&#39;t be able to detect that we need to rebuild the <code>curve</code> bundle.</p><p>There is, however, one file that definitely changed: the lockfile. Thus, by parsing the difference between the lockfile on the master branch and the local lockfile, we can determine which package resolutions changed.</p><p>Then, using the <code>yarn why</code> command, we can determine which package is reliant on those specific resolutions. We can do this recursively until we reach one of our workspace packages, at which point we know that that particular workspace needs to be rebuilt.</p><p>Let us work through the above example:</p><ol><li>Using <code>git diff</code>, we determine that the lockfile has changed relative to the master branch.</li><li>We compare the local lockfile with the master lockfile and determine that that <code>lodash@4.17.0</code> is present in the master lockfile, while <code>lodash@4.17.1</code> is not. So now we know that we need to determine why <code>lodash@4.17.1</code> has been included.</li><li>We run <code>yarn why lodash</code> (without the version since the <code>why</code> command doesn&#39;t accept it) to obtain the list of packages that are dependent on <code>lodash</code>.</li><li>For each package dependent on <code>lodash</code>, we check that that particular package actually depends on <code>lodash@4.17.1</code>, since they might be dependent on other versions of <code>lodash</code>. In this case, we see that <code>gl-matrix@2.0.0</code> needs <code>lodash@4.17.1</code>.</li><li>We do this recursively until we reach our workspace package, and thus determine that it needs to be rebuilt. In this case, we see that the <code>curve</code> bundle requires <code>gl-matrix@2.0.0</code> and thus relies on <code>lodash@4.17.1</code>, so it needs to be rebuilt.</li></ol><h2 id="building-the-dependency-tree" tabindex="-1">Building the Dependency Tree <a class="header-anchor" href="#building-the-dependency-tree" aria-label="Permalink to &quot;Building the Dependency Tree&quot;">​</a></h2><p>Once we determine which workspace packages contain either transitive or non-transitive changes, we must then go through the rest of the workspace packages to determine which other ones need to be rebuilt. From the example above, if the <code>midi</code> bundle needs to be rebuilt because there were changes made to its code, we need to be able to determine that the <code>sound</code> bundle also needs to be rebuilt. And since the <code>sound</code> bundle needs to be rebuilt, then the <code>plotly</code> bundle also needs to be rebuilt.</p><p>This is done by building a simple dependency graph and then creating a topological ordering. Starting with the workspace packages that aren&#39;t dependent on any other workspace packages and working our way to the end of the topological sort, we are able to determine exactly which workspace packages need to be rebuilt.</p><details class="details custom-block"><summary>Circular Dependency Graph</summary><p>A topological sort of course requires that our dependency graph doesn&#39;t contain a cycle. Yarn won&#39;t actually let you create a dependency graph that is cyclic via workspace dependencies, so we&#39;re good to always assume that such a topological sort is possible.</p></details>',10))])}const f=l(p,[["render",k]]);export{w as __pageData,f as default};

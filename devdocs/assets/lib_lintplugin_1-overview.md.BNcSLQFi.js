import{_ as t,c as i,o,a2 as a}from"./chunks/framework.BYVJ-AET.js";const p=JSON.parse('{"title":"Overview","description":"","frontmatter":{"title":"Overview"},"headers":[],"relativePath":"lib/lintplugin/1-overview.md","filePath":"lib/lintplugin/1-overview.md","lastUpdated":1768266602000}'),n={name:"lib/lintplugin/1-overview.md"};function l(s,e,d,r,c,u){return o(),i("div",null,e[0]||(e[0]=[a('<h1 id="modules-eslint-plugin" tabindex="-1">Modules ESLint Plugin <a class="header-anchor" href="#modules-eslint-plugin" aria-label="Permalink to &quot;Modules ESLint Plugin&quot;">​</a></h1><p><code>@sourceacademy/lint-plugin</code> provides the ability for developers to write their own custom linting rules for SA Modules.</p><p>For example, there is a rule that checks that the <code>defineTab</code> helper is being used to help export tabs.</p><p>The plugin also provides several configurations that can be used and extended from for different purposes. For more information, refer to the <a href="./3-config.html">configs</a> section.</p><p>Each of the custom rules made available by this plugin has documentation that follows the typical ESLint rule format. You can find their documentation <a href="./2-rules.html">here</a>.</p><h2 id="building" tabindex="-1">Building <a class="header-anchor" href="#building" aria-label="Permalink to &quot;Building&quot;">​</a></h2><p>Run <code>yarn build</code> to build the plugin. The plugin is bundled into a single <code>dist.js</code> file using <code>esbuild</code>. The types of this plugin have been &quot;hardcoded&quot; into the root <code>index.d.ts</code> file.</p><details class="details custom-block"><summary>Why not just compile to Typescript Declaration Files?</summary><p>The outward facing type interface of this plugin is really only concerned with the rule and config definitions. If we used the compiled output from Typescript, we&#39;d get all the implementation details included within the types for the plugin.</p><p>Instead (and also to save us the time of having to rollup the declaration files), we declare the rules and configs, but mask their internal types using the ESLint provided types (<code>Rule.RuleModule</code> and <code>ESLint.ConfigData</code>).</p></details><h2 id="testing" tabindex="-1">Testing <a class="header-anchor" href="#testing" aria-label="Permalink to &quot;Testing&quot;">​</a></h2><p>Unit testing is provided by <code>vitest</code> with <code>globals: true</code>. This allows both the <code>@typescript-eslint</code> and <code>eslint</code> rule testers to use the <code>describe</code> and <code>test</code> functions without having to set them manually as described in the <a href="https://eslint.org/docs/latest/integrate/nodejs-api#customizing-ruletester" target="_blank" rel="noreferrer"><code>eslint</code> documentation</a>.</p>',10)]))}const f=t(n,[["render",l]]);export{p as __pageData,f as default};

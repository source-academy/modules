import{_ as a,o,i as t,a9 as n}from"./chunks/framework.DuwpaNh0.js";const p=JSON.parse('{"title":"closure_call_sync: An Optional Fast Path for Hot Callback Loops","description":"","frontmatter":{"title":"closure_call_sync: An Optional Fast Path for Hot Callback Loops"},"headers":[],"relativePath":"modules/5-advanced/conductor-interop/5-closure-call-sync.md","filePath":"modules/5-advanced/conductor-interop/5-closure-call-sync.md","lastUpdated":1785666384000}'),s={name:"modules/5-advanced/conductor-interop/5-closure-call-sync.md"};function c(l,e,r,i,h,d){return o(),t("div",null,e[0]||(e[0]=[n('<h1 id="closure-call-sync-an-optional-fast-path-for-hot-callback-loops" tabindex="-1"><code>closure_call_sync</code>: an optional fast path for hot callback loops <a class="header-anchor" href="#closure-call-sync-an-optional-fast-path-for-hot-callback-loops" aria-label="Permalink to &quot;`closure_call_sync`: an optional fast path for hot callback loops&quot;">​</a></h1><p>Conductor&#39;s own <code>ExternCallable</code> contract requires a closure call to be an <code>AsyncGenerator</code> - not a choice either an evaluator or a bundle gets to make, since a call might genuinely need to suspend on a channel round-trip to the frontend. <code>IDataHandler.closure_call</code>/<code>closure_call_unchecked</code> are shaped around that: a bundle <code>yield*</code>s through them, which is also what preserves cadet-code stepping/breakpoint visibility when the closure being called is cadet-authored.</p><p>That&#39;s the right default, but it isn&#39;t free. Even when a call never actually needs to suspend, entering an async generator, wrapping arguments, and awaiting the result costs real, measurable time relative to a plain call - overhead that has nothing to do with any genuine cross-boundary work. It&#39;s negligible for a one-off call. It stops being negligible when a bundle calls a cadet-supplied closure at high frequency - the motivating case is <code>sound</code>&#39;s <code>play()</code>, which samples a cadet-authored wave function up to 44100 times per second of audio.</p><h2 id="the-escape-hatch" tabindex="-1">The escape hatch <a class="header-anchor" href="#the-escape-hatch" aria-label="Permalink to &quot;The escape hatch&quot;">​</a></h2><p><code>IDataHandler.closure_call_sync</code> is an <strong>optional</strong> method: a bundle checks for its presence at runtime and falls back to the async path if it&#39;s missing, rather than assuming any particular evaluator has it. <code>sound</code>&#39;s <code>closureToWave</code> (in <code>index.ts</code>) is the reference implementation of this pattern - it builds the async <code>Wave</code> unconditionally, then separately checks whether <code>evaluator.closure_call_sync</code> exists and, only if so, attaches a matching synchronous <code>.sync</code> method alongside it. Callers that know how to use the fast path (<code>sound</code>&#39;s own sample loop) check for <code>.sync</code> the same way and use it when present; everything else keeps working through the async path unchanged.</p><p>Only <strong>py-slang&#39;s py2js engine</strong> implements <code>closure_call_sync</code> today (PVML and CSE don&#39;t have it yet) - via a closure that&#39;s been dual-compiled into a synchronous and an asynchronous body sharing one closure environment, so whichever side of the boundary picks the function up gets the body it needs. A closure with no synchronous twin, or one whose arguments/result fall outside the sync path&#39;s restricted scalar coverage, makes <code>closure_call_sync</code> return <code>undefined</code> (the &quot;no fast path, use <code>closure_call_unchecked</code> instead&quot; signal) rather than guessing.</p><p><strong>Don&#39;t assume <code>closure_call_sync</code> exists, and don&#39;t assume it doesn&#39;t.</strong> A module has to check for it explicitly every time, not cache the result by evaluator type - a currently-async-only engine (CSE, PVML) could gain the method later, and a specific closure on a supporting engine can still decline (return <code>undefined</code>) if it&#39;s outside the sync path&#39;s coverage.</p><h2 id="attach-sync-liberally-not-selectively" tabindex="-1">Attach <code>.sync</code> liberally, not selectively <a class="header-anchor" href="#attach-sync-liberally-not-selectively" aria-label="Permalink to &quot;Attach `.sync` liberally, not selectively&quot;">​</a></h2><p>Once a closure your bundle hands back to cadet code is provably synchronous - built purely from your own bundle&#39;s math, no real host round-trip involved - attach a <code>.sync</code> twin to it. Do this for every such closure, not just the ones you expect to be sampled in a hot loop by your own bundle&#39;s functions.</p><p>The reason: once a closure is in the cadet&#39;s hands, your bundle has no say over where it goes next. It can be passed straight into a <em>different</em> module&#39;s own synchronous sampling loop, closed over by a cadet-authored combinator, or otherwise re-entered from code your bundle never sees. <code>sound</code>&#39;s own <code>closureToWave</code> (see <code>modules</code>#833&#39;s fix) has to defend against exactly this: a cadet-authored closure whose compiled synchronous body itself calls a different module&#39;s closure can throw partway through a sync probe instead of failing cleanly, if that inner call turns out to need a real round-trip. The safest way to keep that kind of composition working (and fast) is to make sure any closure that <em>can</em> go fully synchronous, does - not to guess at which caller will end up needing it.</p><p>Reserve the <code>AsyncGenerator</code>-only path for closures that genuinely need a real host round-trip - actual I/O, a real async browser API, anything that might have to suspend. Those can&#39;t have a meaningful <code>.sync</code> twin, and that&#39;s correct, permanent behavior for them, not a gap to close.</p><h2 id="why-this-matters-enough-to-have-an-escape-hatch" tabindex="-1">Why this matters enough to have an escape hatch <a class="header-anchor" href="#why-this-matters-enough-to-have-an-escape-hatch" aria-label="Permalink to &quot;Why this matters enough to have an escape hatch&quot;">​</a></h2><p>Numbers from py-slang&#39;s <code>experiments/py2js</code> benchmarks (not load-bearing for correctness, just the reasoning that motivated this design - see py-slang&#39;s own experiment README for methodology and full results if these ever need re-validating):</p><ul><li>Calling convention alone, isolated from any real work: a plain synchronous call runs in the tens of nanoseconds; the mandatory async-generator shape costs on the order of 20× that, purely in allocation and microtask scheduling.</li><li>On the actual sound-module scenario (441 000 wave callbacks, 10s of audio): a synchronous fast path landed within a few percent of a pure-synchronous reference build, while forcing every sample through the async path cost roughly 3× as long per callback.</li><li>Across general execution (not just closure calls), py-slang&#39;s compiled py2js engine measured one to two orders of magnitude faster than the CSE machine and PVML interpreter on the same workloads - PVML and CSE track each other fairly closely, since PVML&#39;s per-instruction interpretation loop and CSE&#39;s control/stash step loop pay similar per-step overhead.</li></ul><p>None of this changes what a module <em>has</em> to do (the async path is always correct, and is the only option on engines without a sync path) - it only explains why a hot per-sample loop is worth the extra branch to check for <code>closure_call_sync</code>, and why that check has to be a runtime capability check rather than an assumption baked in per engine.</p>',15)]))}const y=a(s,[["render",c]]);export{p as __pageData,y as default};

import{_ as o,c as t,o as a,a2 as s}from"./chunks/framework.BYVJ-AET.js";const p=JSON.parse('{"title":"Running from the Desktop","description":"","frontmatter":{},"headers":[],"relativePath":"modules/4-testing/1-desktop.md","filePath":"modules/4-testing/1-desktop.md","lastUpdated":1770529907000}'),n={name:"modules/4-testing/1-desktop.md"};function r(d,e,l,i,c,u){return a(),t("div",null,e[0]||(e[0]=[s('<h1 id="running-from-the-desktop" tabindex="-1">Running from the Desktop <a class="header-anchor" href="#running-from-the-desktop" aria-label="Permalink to &quot;Running from the Desktop&quot;">​</a></h1><p>It is possible to run <code>js-slang</code> directly from the desktop using the <code>js-slang</code> command line. However, this is only really useful for bundles, as <code>js-slang</code> from the command line cannot display tabs.</p><p>You will need to change which module backend is in use using the <code>--modulesBackend</code> parameter.</p><p>If your package already depends on <code>js-slang</code>, then you can already run <code>js-slang</code> from the command line using <code>yarn js-slang</code>.</p><p>If not, you can either:</p><ul><li>Add <code>js-slang</code> to your dev dependencies</li><li>Install the root repository package.</li><li>Use the <a href="https://yarnpkg.com/cli/dlx" target="_blank" rel="noreferrer"><code>yarn dlx</code> command</a></li></ul><h2 id="the-modules-server" tabindex="-1">The Modules Server <a class="header-anchor" href="#the-modules-server" aria-label="Permalink to &quot;The Modules Server&quot;">​</a></h2><p>All modules related files are served from a single HTTP server. For <code>js-slang</code> to be able to access your local modules copies, you will have to run a local version of this server and then point <code>js-slang</code> to it.</p><p>The <code>yarn buildtools serve</code> command will start a HTTP server and serve the <code>build</code> directory, which is where your bundle/tab should&#39;ve been output to after compilation.</p><p>If need be, you can use the <code>--port</code> option or the <code>PORT</code> environment variable to change what port the server listens on, the default being 8022.</p><p>Similarly, <code>--bind</code> can be used to change which interface the server listens on.</p><div class="warning custom-block github-alert"><p class="custom-block-title">On Focused Installs</p><p>The HTTP server is provided by the buildtools and thus is only available within packages that have installed the buildtools. If you used a focused Yarn install, then the <code>serve</code> command is only available within your bundle&#39;s/tab&#39;s package.</p><p>Using a focused install also means that other bundles and tabs are not available for use during your testing, as they will not be compiled.</p><p>If the <code>serve</code> command is unavailable for whatever reason, you can install it with the root repository using <code>yarn workspaces focus @sourceacademy/modules</code> and then run <code>yarn serve</code> from the root repository.</p></div>',12)]))}const m=o(n,[["render",r]]);export{p as __pageData,m as default};

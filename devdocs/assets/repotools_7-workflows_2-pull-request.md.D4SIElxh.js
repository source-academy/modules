import{_ as t,c as o,o as a,a2 as s}from"./chunks/framework.BYVJ-AET.js";const p=JSON.parse('{"title":"On Pull Request","description":"","frontmatter":{"title":"On Pull Request"},"headers":[],"relativePath":"repotools/7-workflows/2-pull-request.md","filePath":"repotools/7-workflows/2-pull-request.md","lastUpdated":1762684265000}'),r={name:"repotools/7-workflows/2-pull-request.md"};function i(n,e,l,d,c,h){return a(),o("div",null,e[0]||(e[0]=[s('<h1 id="the-pull-request-workflow" tabindex="-1">The Pull Request Workflow <a class="header-anchor" href="#the-pull-request-workflow" aria-label="Permalink to &quot;The Pull Request Workflow&quot;">​</a></h1><p>The main Github Action that runs on for each pull request to verify the state of the repository consists of several separate &quot;jobs&quot;.</p><p>This workflow makes use of <code>matrix</code> strategy to run the tasks in parallel where possible.</p><h2 id="_1-find-packages" tabindex="-1">1. <code>find-packages</code> <a class="header-anchor" href="#_1-find-packages" aria-label="Permalink to &quot;1. `find-packages`&quot;">​</a></h2><p>This job executes first, using the <a href="./1-actions.html#information-action-infoactionyml">Information Action</a>. The action returns all the necessary information for the subsequent jobs to execute their tasks.</p><div class="info custom-block github-alert"><p class="custom-block-title">INFO</p><p>The info action runs <code>git --no-pager diff master [directory]</code> from a package&#39;s root directory to determine if there are any changes in the package. Because of this, it is also necessary to run a <code>git fetch</code>.</p></div><h2 id="_2-libraries-bundles-and-tabs" tabindex="-1">2. <code>libraries</code>, <code>bundles</code> and <code>tabs</code> <a class="header-anchor" href="#_2-libraries-bundles-and-tabs" aria-label="Permalink to &quot;2. `libraries`, `bundles` and `tabs`&quot;">​</a></h2><p>These three jobs are matrix jobs that run tasks for the individual packages. If the package has changes, then the job will run tests and <code>tsc</code> for the package.</p><p>This means that if the package requires <code>playwright</code> to run its tests, it will be installed during the init step.</p><p>These jobs have the <code>fail-fast</code> option set to <code>false</code>, so a single job failing doesn&#39;t mean that the rest of the jobs in the workflow get cancelled.</p><p>For libraries in particular, the job is run with both the <code>ubuntu-latest</code> and <code>windows-latest</code> OSes. This makes sure that our libraries are compatibile with both Windows and POSIX-Compliant operating systems.</p><h2 id="_3-devserver" tabindex="-1">3. <code>devserver</code> <a class="header-anchor" href="#_3-devserver" aria-label="Permalink to &quot;3. `devserver`&quot;">​</a></h2><p>The <code>devserver</code> jobs only executes if there were changes within the dev server. Since it relies on bundles and tabs being compiled, it does rely on both the <code>bundles</code> and the <code>tabs</code> jobs to have completed.</p><h2 id="_4-docserver" tabindex="-1">4. <code>docserver</code> <a class="header-anchor" href="#_4-docserver" aria-label="Permalink to &quot;4. `docserver`&quot;">​</a></h2><p>The <code>docserver</code> job only executes if there were changes made to the modules library or the docs themselves. It doesn&#39;t deploy the docs here, so it the call to <code>yarn build</code> only serves as to way to check that the docs can be generated without errors like dead links.</p><h2 id="_5-repo-tasks" tabindex="-1">5. <code>repo-tasks</code> <a class="header-anchor" href="#_5-repo-tasks" aria-label="Permalink to &quot;5. `repo-tasks`&quot;">​</a></h2><p>This job consists of checking <code>yarn constraints</code> and running ESLint for the entire repository. It doesn&#39;t rely on any of the other jobs, so it runs in parallel to all of them.</p>',17)]))}const f=t(r,[["render",i]]);export{p as __pageData,f as default};

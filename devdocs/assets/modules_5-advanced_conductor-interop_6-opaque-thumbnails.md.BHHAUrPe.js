import{_ as a,o as s,i,a9 as t}from"./chunks/framework.DuwpaNh0.js";const k=JSON.parse('{"title":"Stepper Thumbnails for Opaque Values","description":"","frontmatter":{"title":"Stepper Thumbnails for Opaque Values"},"headers":[],"relativePath":"modules/5-advanced/conductor-interop/6-opaque-thumbnails.md","filePath":"modules/5-advanced/conductor-interop/6-opaque-thumbnails.md","lastUpdated":1786067936000}'),n={name:"modules/5-advanced/conductor-interop/6-opaque-thumbnails.md"};function o(r,e,l,h,d,p){return s(),i("div",null,e[0]||(e[0]=[t(`<h1 id="stepper-thumbnails-for-opaque-values" tabindex="-1">Stepper thumbnails for opaque values <a class="header-anchor" href="#stepper-thumbnails-for-opaque-values" aria-label="Permalink to &quot;Stepper thumbnails for opaque values&quot;">​</a></h1><p>A <code>DataType.OPAQUE</code> value (see <a href="./3-opaque.html">where recursion stops</a>) is, by design, inert to the evaluator - it&#39;s handed back out exactly as given, with no attempt to interpret it. That&#39;s exactly right for something like a <code>binary_tree</code> payload, but it leaves a gap for opaque values that are genuinely <em>visual</em> - a <code>rune</code>&#39;s WebGL scene graph, a future <code>curve</code>&#39;s parametric plot - where a stepper walking through a cadet&#39;s program would rather show a small picture than the text <code>&lt;opaque&gt;</code>.</p><p>Unlike a bitmap image type (DrRacket&#39;s <code>2htdp/image</code> values, for comparison), these opaque values have no pixels sitting in memory - they&#39;re descriptions of a scene that only produce pixels once something drives an actual draw call through a canvas/GPU context. So &quot;thumbnail an opaque value&quot; needs a real render step, and that step can only live inside the module that owns the value - only <code>rune</code> knows how to turn its own scene graph into pixels.</p><h2 id="the-convention-the-value-carries-its-own-renderer" tabindex="-1">The convention: the value carries its own renderer <a class="header-anchor" href="#the-convention-the-value-carries-its-own-renderer" aria-label="Permalink to &quot;The convention: the value carries its own renderer&quot;">​</a></h2><p>Rather than a module-level registry, or a new Python-callable export, a module that wants stepper thumbnails attaches a zero-argument render function directly onto the JS object it already passes to <code>opaque_make</code>, under a dedicated <code>Symbol</code> key exported from <code>@sourceacademy/modules-lib</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { RENDER_THUMBNAIL_SYMBOL } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@sourceacademy/modules-lib/conductor/thumbnail&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Object.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defineProperty</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(value, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">RENDER_THUMBNAIL_SYMBOL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> renderToDataUrl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(value),   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// () =&gt; Promise&lt;string | undefined&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  enumerable: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  configurable: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><h3 id="why-this-shape" tabindex="-1">Why this shape <a class="header-anchor" href="#why-this-shape" aria-label="Permalink to &quot;Why this shape&quot;">​</a></h3><ul><li><strong>No new public API.</strong> The hook never touches <code>exports</code>/the module&#39;s Python-facing surface - cadet code can&#39;t call it, doesn&#39;t know it exists, and none of the module&#39;s exported functions change signature.</li><li><strong>No SDK changes needed.</strong> <code>opaque_make</code>/<code>opaque_get</code> hand back the exact same live in-memory JS reference, in the same realm - a plain extra property just rides along, no serialization boundary to survive.</li><li><strong>No provenance/registry bookkeeping for the consumer.</strong> A stepper doesn&#39;t need to track &quot;this value came from module X&quot; and look up a renderer by name - the value already carries what it needs.</li><li><strong><code>Symbol.for</code>, not a plain string or a <code>Symbol()</code>.</strong> <code>Symbol.for(&#39;source-academy.stepper.renderThumbnail&#39;)</code> is a <em>global-registry</em> symbol - a module and a consumer in entirely different packages/repos (this repo&#39;s <code>rune</code>, py-slang&#39;s stepper) get the identical key without importing from each other. Only the string itself is the real cross-repo contract; <code>RENDER_THUMBNAIL_SYMBOL</code> is exported from <code>modules-lib</code> purely so nobody has to hand-type it and risk a typo.</li></ul><h3 id="the-contract" tabindex="-1">The contract <a class="header-anchor" href="#the-contract" aria-label="Permalink to &quot;The contract&quot;">​</a></h3><ul><li><strong>Signature:</strong> <code>() =&gt; Promise&lt;string | undefined&gt;</code>. Resolves to a data URL (e.g. <code>data:image/png;base64,...</code>) on success, or to <code>undefined</code> if a render attempt fails at call time - consumers must handle both, never assume the result is always a valid string.</li><li><strong>Attach only when actually renderable, never throw.</strong> If rendering isn&#39;t possible in the current realm (see below), simply don&#39;t attach the property at all - a consumer that doesn&#39;t find the key falls back to a generic placeholder (ideally the value&#39;s constructor name, e.g. <code>&lt;Rune&gt;</code>, obtainable for free via <code>v?.constructor?.name</code> on the same <code>opaque_get</code> payload). If the hook <em>is</em> attached but a render attempt fails at call time, resolve to <code>undefined</code> rather than rejecting - a broken thumbnail must never surface as a runtime error to cadet code, and a consumer that gets <code>undefined</code> back should fall back to the same placeholder it&#39;d use for a missing hook.</li><li><strong>Mutate the value in place, don&#39;t wrap or copy it.</strong> Whatever object shape <code>opaque_make</code> receives (e.g. <code>rune</code>&#39;s <code>Rune</code> class instances) very likely has other code checking <code>value instanceof SomeClass</code> on round-trips through <code>opaque_get</code> - a spread copy or wrapper object would silently break those checks. <code>Object.defineProperty(value, ..., { enumerable: false })</code> on the original object avoids that, and keeps the hook out of <code>for...in</code>/<code>Object.keys</code>/hand-written serialization that reads named fields explicitly.</li></ul><h2 id="no-dom-canvas-in-the-render-realm" tabindex="-1">No DOM canvas in the render realm <a class="header-anchor" href="#no-dom-canvas-in-the-render-realm" aria-label="Permalink to &quot;No DOM canvas in the render realm&quot;">​</a></h2><p>The module plugin runs in the same realm as the evaluator - which, in production, is a Web Worker, not the main thread. There&#39;s no <code>document</code>, no <code>HTMLCanvasElement</code> there; all of a bundle&#39;s normal canvas/WebGL rendering happens host-side (e.g. <code>rune</code>&#39;s tab, fed over its <code>IChannel</code>), fed by a real, mounted DOM canvas. A thumbnail hook has to produce pixels <em>without</em> any of that.</p><p><code>rune</code>&#39;s implementation (<code>src/bundles/rune/src/rune.ts</code>) uses <code>OffscreenCanvas</code>, which is available inside Web Workers and supports both <code>getContext(&#39;webgl&#39;)</code> and <code>convertToBlob()</code>. Its existing WebGL draw code (<code>getWebGlFromCanvas</code>, <code>drawRunesToFrameBuffer</code>, <code>DrawnNormalRune.draw</code>) was widened to accept <code>HTMLCanvasElement | OffscreenCanvas</code> rather than duplicated, so the exact same shader/buffer logic serves both the tab&#39;s live rendering and the headless thumbnail render.</p><p><code>OffscreenCanvas</code> + WebGL + <code>convertToBlob</code> support isn&#39;t universal across every environment an evaluator might run in. <code>rune</code> feature-detects (<code>typeof OffscreenCanvas !== &#39;undefined&#39;</code>) before attaching the hook at all, and wraps the actual render in a try/catch that resolves to <code>undefined</code> on any failure - see the &quot;attach only when renderable, never throw&quot; rule above.</p><h2 id="adopting-this-in-another-module" tabindex="-1">Adopting this in another module <a class="header-anchor" href="#adopting-this-in-another-module" aria-label="Permalink to &quot;Adopting this in another module&quot;">​</a></h2><p>A sketch for a hypothetical <code>curve</code> adoption, following the exact same shape as <code>rune</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { RENDER_THUMBNAIL_SYMBOL } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@sourceacademy/modules-lib/conductor/thumbnail&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> attachThumbnailHook</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">curve</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Curve</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Curve</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">typeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> OffscreenCanvas </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!==</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;undefined&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Object.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defineProperty</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(curve, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">RENDER_THUMBNAIL_SYMBOL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">      value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> renderCurveThumbnail</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(curve),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      enumerable: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      configurable: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> curve;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// wherever the module currently calls opaque_make(curve, ...):</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.evaluator.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opaque_make</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">attachThumbnailHook</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(curve), </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p><code>renderCurveThumbnail</code> is the one genuinely module-specific piece - reuse whatever WebGL/canvas rendering code the module already has (widened to accept <code>OffscreenCanvas</code>, the same way <code>rune</code> did), rather than writing a second, parallel rendering path.</p><h2 id="scope-note" tabindex="-1">Scope note <a class="header-anchor" href="#scope-note" aria-label="Permalink to &quot;Scope note&quot;">​</a></h2><p>This page documents the <em>module</em> side of the convention only. The consumer - a stepper actually reading <code>RENDER_THUMBNAIL_SYMBOL</code> off an opaque value and rendering an inline <code>&lt;img&gt;</code> - lives in py-slang, is tracked there separately, and is itself gated on the stepper supporting <code>import</code> at all. A module that attaches this hook ships with no visible effect on its own until that consumer lands.</p>`,20)]))}const u=a(n,[["render",o]]);export{k as __pageData,u as default};

import{_ as a,o as t,i as o,a9 as n}from"./chunks/framework.DuwpaNh0.js";const p=JSON.parse('{"title":"Pairs and Arrays Are the Same Thing","description":"","frontmatter":{"title":"Pairs and Arrays Are the Same Thing"},"headers":[],"relativePath":"modules/5-advanced/conductor-interop/2-pairs-and-arrays.md","filePath":"modules/5-advanced/conductor-interop/2-pairs-and-arrays.md","lastUpdated":1785659156000}'),i={name:"modules/5-advanced/conductor-interop/2-pairs-and-arrays.md"};function r(s,e,d,c,l,h){return t(),o("div",null,e[0]||(e[0]=[n('<h1 id="pairs-and-arrays-there-is-no-pair-type" tabindex="-1">Pairs and arrays: there is no &quot;pair&quot; type <a class="header-anchor" href="#pairs-and-arrays-there-is-no-pair-type" aria-label="Permalink to &quot;Pairs and arrays: there is no &quot;pair&quot; type&quot;">​</a></h1><blockquote><p>&quot;there is nothing called a pair anymore. Pairs are just arrays of length 2.&quot; — Martin Henz</p></blockquote><p>Conductor&#39;s <code>DataType.PAIR</code> still exists at the protocol level as of this writing (<code>pair_make</code>/<code>pair_head</code>/<code>pair_tail</code>/<code>pair_assert</code> are real <code>IDataHandler</code> methods, and plenty of already-migrated bundles call them), but it is <strong>not a separate concept from <code>DataType.ARRAY</code></strong> as far as an evaluator&#39;s own conversion layer is concerned. The only places &quot;pair&quot; survives as a distinct idea at all:</p><ul><li>The <em>name</em> of Python builtins (<code>pair()</code>, <code>is_pair()</code>, etc.) — a chapter-2/SICPy vocabulary choice, not a protocol-level distinction. (In Python §2/SICPy/CS1101S Unit 2, &quot;pair&quot; is the name given to a 2-element Python list, precisely because Python lists as a general concept aren&#39;t introduced until §3/Unit 3.)</li><li>Chapter 1-2 error-message wording (&quot;expected pair&quot;, not &quot;expected list&quot;) — purely cosmetic, the same value either way.</li></ul><div class="warning custom-block"><p class="custom-block-title">DataType.PAIR is on track for full removal from py-slang</p><p>On the PR that settled this (<a href="https://github.com/source-academy/py-slang/pull/307" target="_blank" rel="noreferrer">py-slang#307</a>), Martin went further than &quot;treat them as interchangeable&quot;:</p><blockquote><p>&quot;We still have DataType.PAIR in the code base. I suggest we remove the datatype entirely from py-slang. [...] I don&#39;t see value in DataType.PAIR in py-slang. It just leads to confusion. Implementers will ask: what is the difference between a PAIR and an ARRAY with two elements?&quot;</p></blockquote><p>A follow-up PR has been claimed to actually remove <code>DataType.PAIR</code> from py-slang&#39;s own code, rather than merely bridging both representations the way <a href="https://github.com/source-academy/py-slang/pull/307" target="_blank" rel="noreferrer">py-slang#307</a> currently does. Once that lands, py-slang will never construct or expect a <code>DataType.PAIR</code> value itself — a bundle that still calls <code>pair_make</code>/<code>pair_head</code>/<code>pair_tail</code> internally should keep working (module implementers remain free to define their own <code>head</code>/<code>tail</code>-style functions), but should not expect py-slang&#39;s <em>own</em> engines to ever hand it something tagged <code>DataType.PAIR</code>.</p></div><p>Until that follow-up lands, everywhere in py-slang&#39;s own conversion code, a pair <em>is</em> treated as a 2-element array, interchangeably:</p><ul><li>A <code>DataType.ARRAY</code>-tagged value can be read via <code>pair_head</code>/<code>pair_tail</code> (reading index 0/1) exactly as a genuine <code>DataType.PAIR</code> can — bundle code that calls these for clarity keeps working unchanged, whichever representation the value actually carries at runtime.</li><li>The generic list helpers (<code>is_list</code>/<code>list_to_vec</code>/<code>length</code>/<code>accumulate</code>) accept both a <code>DataType.PAIR</code>/<code>EMPTY_LIST</code> chain and a flat <code>DataType.ARRAY</code> — a bundle that only ever calls these needs no changes at all.</li><li>An evaluator&#39;s Python-list-literal-to-module conversion builds a flat <code>DataType.ARRAY</code> for <strong>any</strong> Python list, regardless of length — including length 2, and including an empty list (which becomes a genuine 0-length array, not <code>EMPTY_LIST</code> — see the warning below on why that distinction matters). There is no length-based special case, and no need to track &quot;was this value a genuine list literal, or the result of <code>pair()</code>/<code>llist()</code>, or a module value round-tripped back through the language&quot; the way earlier code did.</li></ul><h2 id="untyped-and-recursive" tabindex="-1">Untyped and recursive <a class="header-anchor" href="#untyped-and-recursive" aria-label="Permalink to &quot;Untyped and recursive&quot;">​</a></h2><p><code>DataType.ARRAY</code> should be treated as <strong>untyped</strong> by the evaluator&#39;s own conversion code: don&#39;t gate behavior on what an array claims its element type is. An array can be heterogeneous — <code>sound</code>&#39;s <code>Sound</code> is exactly <code>(wave, duration)</code>, a 2-element pair whose first element is a function and second is a number, and there&#39;s no reason an evaluator&#39;s generic array-handling code should assume otherwise. Convert recursively, element by element, with no assumptions about homogeneity.</p><h2 id="the-bug-this-was-fixing" tabindex="-1">The bug this was fixing <a class="header-anchor" href="#the-bug-this-was-fixing" aria-label="Permalink to &quot;The bug this was fixing&quot;">​</a></h2><p>Before this, py-slang&#39;s Python-list-to-module conversion built a linked <code>PAIR</code>/<code>EMPTY_LIST</code> chain for a list literal, and used a same-length ambiguity heuristic (was this value tagged as &quot;a genuine list literal&quot; at construction time, or not?) to decide whether a 2-element value should become a proper terminated chain or a raw dotted pair. That heuristic is fundamentally unreliable once a value round-trips out to the language and back in: a generic, untyped module function (one with no declared <code>LIST</code>/<code>ARRAY</code> parameter type to hint at the right shape — e.g. <code>repeat</code>&#39;s <code>identity</code>/<code>composition</code>, which just relay whatever they&#39;re given) had no way to distinguish the two cases either, and <code>identity([0])</code> printed <code>[0.0, None]</code> instead of <code>[0.0]</code> — an extra <code>None</code> from the chain terminator being misread as a real list element.</p><p>Building every Python list as a flat <code>ARRAY</code> (rather than ever building a <code>PAIR</code> chain for one) removes the ambiguity at the source: an array&#39;s length is explicit, never inferred by walking a chain to a terminator, so there&#39;s nothing left to misread.</p><div class="warning custom-block github-alert"><p class="custom-block-title">Empty list vs None</p><p><code>DataType.EMPTY_LIST</code> is <em>also</em> what a module function&#39;s <code>None</code>/void return maps to (see <a href="./1-numbers.html">Numbers are always floats</a>&#39;s neighbouring cases, or any engine&#39;s <code>moduleToPython</code>). An empty Python list must <strong>not</strong> become <code>EMPTY_LIST</code> on the way into a module — that makes <code>[]</code> and <code>None</code> indistinguishable on the way back out. It should become a genuine 0-length <code>DataType.ARRAY</code> instead, exactly like any other length. This was caught by live testing after an initial version of the fix got this wrong (see the &quot;was <code>None</code>, now <code>[]</code>&quot; regression tests in py-slang&#39;s <code>pvml-modules.test.ts</code>).</p></div>',13)]))}const y=a(i,[["render",r]]);export{p as __pageData,y as default};

import{_ as t,o as a,c as i,a2 as o}from"./chunks/framework.BYVJ-AET.js";const p=JSON.parse('{"title":"Custom Actions","description":"","frontmatter":{"title":"Custom Actions"},"headers":[],"relativePath":"repotools/7-workflows/1-actions.md","filePath":"repotools/7-workflows/1-actions.md","lastUpdated":1772619416000}'),n={name:"repotools/7-workflows/1-actions.md"};function s(c,e,r,l,h,d){return a(),i("div",null,e[0]||(e[0]=[o('<h1 id="custom-github-actions" tabindex="-1">Custom Github Actions <a class="header-anchor" href="#custom-github-actions" aria-label="Permalink to &quot;Custom Github Actions&quot;">​</a></h1><p>Github Actions does provide us the ability to write our own custom actions that can be integrated with the rest of the pipeline. This repository makes use of two:</p><ul><li>Initializer Action</li><li>Information Action</li></ul><p>Creating custom actions is detailed <a href="https://docs.github.com/en/actions/reference/workflows-and-actions/metadata-syntax" target="_blank" rel="noreferrer">here</a>. The convention for each action is have its metadata file (<code>action.yml</code>) in the same directory as its source code (if necessary).</p><h2 id="information-action-info-action-yml" tabindex="-1">Information Action (<code>info/action.yml</code>) <a class="header-anchor" href="#information-action-info-action-yml" aria-label="Permalink to &quot;Information Action (`info/action.yml`)&quot;">​</a></h2><p>The information action retrieves information about each package present in the repository, namely:</p><ul><li>Has this package changed relative to the master branch?</li><li>Name of the package</li><li>Directory of the package</li><li>Does the package require <code>playwright</code>?</li></ul><p>In the case of bundles or tabs, it also retrieves the name of the bundle or tab.</p><p>In case of changes to the lockfile, it also determines which packages need to be rebuilt. This can happen when dependencies of dependencies change, so no change is present in the package&#39;s source code itself, but the lockfile itself has changed.</p><p>More detail about detecting changes can be found in <a href="./4-changes.html">this section</a>.</p><p>This information is used by both the initializer action and the workflows to determine the what tasks need to be executed.</p><h2 id="initializer-action-init-action-yml" tabindex="-1">Initializer Action (<code>init/action.yml</code>) <a class="header-anchor" href="#initializer-action-init-action-yml" aria-label="Permalink to &quot;Initializer Action (`init/action.yml`)&quot;">​</a></h2><p>The initializer action combines the initialization steps that are necessary for each package:</p><ol><li>Enable <code>corepack</code></li><li>Install dependencies for that workspace (using <code>yarn workspaces focus</code>)</li><li>Install playwright if necessary (using <code>yarn playwright install --with-deps</code>)</li></ol><div class="info custom-block github-alert"><p class="custom-block-title">What about actions/checkout?</p><p>Theoretically this action should also include <code>actions/checkout</code> since this action is repeated across the different packages. However, the code for this action sits in the repository, so for Github to be able to execute the init action, it needs to first obtain its code, which means the repository must be checked out before this action can run.</p><p>Thus, the initializer action has to run <em>after</em> <code>actions/checkout</code> has completed and so it has to be a separate step.</p></div><h2 id="load-artifacts-action-load-artifacts-action-yml" tabindex="-1">Load Artifacts Action (<code>load-artifacts/action.yml</code>) <a class="header-anchor" href="#load-artifacts-action-load-artifacts-action-yml" aria-label="Permalink to &quot;Load Artifacts Action (`load-artifacts/action.yml`)&quot;">​</a></h2><p>When the CI executes, it will build any tab that has changes relative to the master branch. If the devserver also has changes, then its tests will run. Since the devserver requires the compiled version of all tabs, we must build all tabs before running the devserver&#39;s tests.</p><p>Since some tabs might already have been built, there is no reason for us to go through the lengthy process of installing those tabs&#39; dependencies and rebuilding them. Instead, if we save the built tabs as workflow artifacts, we can restore those tabs before running the devserver&#39;s tasks. All that remains then is to build the tabs that haven&#39;t already been built.</p><p>This action does this exact job: figuring out which tabs have already been built successfully and thus can be downloaded and which tabs need to be built from scratch.</p>',19)]))}const f=t(n,[["render",s]]);export{p as __pageData,f as default};
